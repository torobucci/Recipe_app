<main>
    <h1 class="title">Shopping List</h1>
    <% if @mfoods.blank? %>
    <p>Your shopping list is currently empty!</p>
    <% else %>
    <div class="shopping_list_p flex">
        <p>Amount of food items to buy: <strong><%= @foods.count%></strong></p>
        <p>Total value of food needed: <strong>$<%= @foods.sum { |food| food.price * food.quantity } %></strong></p>
    </div>
    <table class="tables">
        <tr>
          <th>Food</th>
          <th>Quantity</th>
          <th>Price</th>
       </tr>
    <% @foods.each do |food| %>
       <tr>
         <td><%= food.name%></td>
         <td><%= food.quantity%><%= food.measurement_unit%></td>
         <td>$<%= food.price%></td>
       </tr>
    <% end %>
    </table>
    <% end %>
</main>
